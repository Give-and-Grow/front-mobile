# front-mobile

test

```bash
git branch
git add .
git commit -m "test"
git push origin اسم البرانش
--Create Pull Request
--Merge Pull Request
git checkout main
git pull origin main
git branch -d  اسم البرانش
git push origin --delete  اسم البرانش
git checkout -b جديد اسم البرانش


git checkout البرانش
git branch
--------------------------------------
backend:
cd Backend
git branch
venv\Scripts\activate
git pull origin main
pip install -r requirements.txt
flask db upgrade
بنشوف الداتا بيس
python run.py

-------------------

```
